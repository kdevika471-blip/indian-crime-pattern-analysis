{
  "name": "crime-analysis-mobile",
  "version": "1.0.0",
  "description": "Android Mobile conversion for Crime Analysis Flask App",
  "main": "app.py",
  "scripts": {
    "build": "echo 'No build step required for Flask app.'",
    "cap-init": "npx cap init",
    "cap-copy": "npx cap copy",
    "cap-sync": "npx cap sync",
    "cap-open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/geolocation": "^5.0.0",
    "@capacitor-community/call-number": "^5.0.0",
    "capacitor-sms": "^1.0.0",
    "@capacitor-community/background-geolocation": "^1.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
